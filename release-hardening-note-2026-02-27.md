# Release Hardening Note (2026-02-27)

## 目的
未完タスク「収益化プロトタイプの公開とテスト」を前進させるため、公開前の入力品質ガードを実装しました。

## 実施内容（60分枠）
- `ops_audit_cli.py` に入力バリデーションを追加。
  - 必須8項目の存在チェック
  - 型チェック（数値/文字列）
  - 値域チェック（`retry_rate`/`error_rate` は 0〜1、`requests` は 0以上）
- 失敗時の終了コードを明確化。
  - `2`: 入力ファイル未発見
  - `3`: JSON不正
  - `4`: 入力バリデーション不正

## テスト結果
- 正常系: `sample_input.json` → `sample_output.json` 生成成功（exit=0）
- 異常系: 必須項目欠落JSON → `Validation error`（exit=4）

## 期待効果
- 公開後の「フォーマット揺れ」での誤判定を事前遮断。
- 外部利用時のデバッグ時間短縮。
- βテスト時に入力不備とロジック不備を分離して記録可能。
